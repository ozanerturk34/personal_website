{
  "name": "grpc",
  "version": "1.0.0",
  "description": "Grpc package to create proto type, client and server stubs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:proto": "./protogen.sh",
    "addPermissionProtogen": "chmod +x ./protogen.sh"
  },
  "author": "",
  "devDependencies": {
    "@types/google-protobuf": "^3.15.6",
    "ts-protoc-gen": "^0.15.0"
  },
  "dependencies": {
    "google-protobuf": "^3.21.2"
  }
}
